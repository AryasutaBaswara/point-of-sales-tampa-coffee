import Login from "./_components/login";

export const metadata = {
  title: "Login | POS Cafe",
  description: "Login to access the POS Cafe application.",
};

export default function LoginPage() {
  return <Login />;
}
