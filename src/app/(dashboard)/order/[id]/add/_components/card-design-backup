<!-- <Card className="group flex flex-col h-full overflow-hidden border-border hover:shadow-lg transition-all duration-300">
      {/* BAGIAN HEADER / GAMBAR */}
      <CardHeader className="p-0 relative aspect-4/3 overflow-hidden bg-muted">
        <Image
          src={menu.image_url as string} // Pastikan type string
          alt={menu.name}
          fill
          sizes="(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 33vw"
          className="object-cover transition-transform duration-500 group-hover:scale-105"
        />
        {/* Opsional: Badge Kategori di pojok gambar */}
        <div className="absolute top-2 left-2">
          <Badge
            variant="secondary"
            className="bg-black/50 text-white hover:bg-black/70 backdrop-blur-sm border-none"
          >
            {menu.category}
          </Badge>
        </div>
      </CardHeader>

      {/* BAGIAN KONTEN (Judul & Deskripsi) */}
      <CardContent className="flex flex-col flex-1 p-4 gap-2">
        <h3
          className="font-bold text-lg leading-tight group-hover:text-primary transition-colors line-clamp-1"
          title={menu.name}
        >
          {menu.name}
        </h3>
        <p className="text-sm text-muted-foreground line-clamp-2 leading-relaxed">
          {menu.description}
        </p>
      </CardContent>

      {/* BAGIAN FOOTER (Harga & Tombol) */}
      <CardFooter className="p-4 pt-0 flex justify-between items-center mt-auto">
        <div className="flex flex-col">
          <span className="text-xs text-muted-foreground font-medium">
            Price
          </span>
          <span className="text-lg font-bold text-foreground">
            {convertIDR(menu.price)}
          </span>
        </div>

        <Button
          size="icon"
          className="rounded-full shadow-sm active:scale-95 transition-transform"
        >
          {/* Bisa pilih mau icon Plus (+) atau Keranjang */}
          <Plus className="w-5 h-5" />
        </Button>
      </CardFooter>
    </Card> -->
